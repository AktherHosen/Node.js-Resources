const { add, substract } = require("./utils"); //when we donot set related path js find by default search index.js so that is why we don't need to specify index.js

console.log(add(2, 3));
console.log(substract(5, 3));
